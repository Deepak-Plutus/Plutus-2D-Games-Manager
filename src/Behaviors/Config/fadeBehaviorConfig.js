/** @see https://www.construct.net/en/make-games/manuals/construct-3/behavior-reference/fade */
export const fadeBehaviorDefaults = {
  /** Seconds */
  fadeInDuration: 0,
  waitDuration: 0,
  fadeOutDuration: 1,
  /** Restart cycle when fade-out completes */
  loop: false,
  destroyOnComplete: false,
  /** Delay before starting (s) */
  startDelay: 0,
  /** GSAP ease string */
  ease: 'none',
};
