/**
 * Default settings for `FadeBehavior`.
 *
 * @type {const}
 */
export const fadeBehaviorDefaults = {
  fadeInDuration: 0,
  waitDuration: 0,
  fadeOutDuration: 1,
  loop: false,
  destroyOnComplete: false,
  startDelay: 0,
  ease: 'none'
}
