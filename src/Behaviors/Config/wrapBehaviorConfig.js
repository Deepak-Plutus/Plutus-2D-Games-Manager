/** @see https://www.construct.net/en/make-games/manuals/construct-3/behavior-reference/wrap */
export const wrapBehaviorDefaults = {
  /** `layout` | `viewport` (viewport uses layout size in this engine) */
  wrapTo: 'layout',
  wrapHorizontal: true,
  wrapVertical: true,
  /** Extra inset from edges when re-positioning (px) */
  margin: 0,
};
