/**
 * Default settings for `FlashBehavior`.
 *
 * @type {const}
 */
export const flashBehaviorDefaults = {
  onDuration: 0.1,
  offDuration: 0.1,
  repeatCount: 3,
  flashTint: 'ffffff',
  restoreTint: 'ffffff',
  ease: 'power2.inOut'
}
